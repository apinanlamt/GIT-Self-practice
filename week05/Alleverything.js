export default function getLength(str) { return str.length; }
export function echo(msg) { return msg; }
export const TAX = 0.07;
export class Book {
  constructor(isbn, title) {
    this.isbn = isbn;
    this.title = title;
  }
}
